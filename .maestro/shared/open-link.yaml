appId: ${APP_ID}
---
- openLink: ${URL}
- waitForAnimationToEnd
- runFlow:
    when:
      platform: iOS
    commands:
      - runFlow:
          when:
            visible: Open
          commands:
            - tapOn: Open
